<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ADS-SJSU</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>:root{ --bs-primary:#003E7E; --bs-primary-rgb:0,62,126; }</style>
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">ADS-SJSU</a>
      <div class="ms-auto">
        <% if (user) { %>
          <a class="btn btn-outline-light me-2" href="/dashboard">Dashboard</a>
          <a class="btn btn-outline-light" href="/logout">Logout</a>
        <% } else { %>
          <a class="btn btn-light text-primary" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container py-5 text-center">
    <% if (alert) { %>
      <div class="alert alert-<%= alert.type %> mb-4"><%= alert.text %></div>
    <% } %>

    <h1 class="display-5 fw-bold text-primary mb-3">
      Welcome to the Department of Applied Data Science
    </h1>

    <% if (!user) { %>
      <p class="lead text-secondary">Please log in to view your dashboard.</p>
      <a class="btn btn-lg btn-primary mb-4" href="/login">Login</a>
    <% } else { %>
      <p class="lead text-secondary">Open your dashboard to see your courses and session details.</p>
      <a class="btn btn-lg btn-warning text-dark mb-4" href="/dashboard">Go to your Dashboard</a>
    <% } %>

    <!-- Image UNDER the button -->
    <img
      src="https://media.licdn.com/dms/image/v2/D5622AQF6yXt3tt64Uw/feedshare-shrink_800/B56ZSviLqoGoAg-/0/1738111763011?e=2147483647&v=beta&t=Mur180a4Dx-XxoMaqEM8UbyGSLpk5lebRCbIc0hAtqQ"
      alt="SJSU Applied Data Science"
      class="img-fluid rounded-4 shadow mx-auto d-block"
      style="max-width: 980px; width: 100%;"
    />
  </div>
</body>
</html>
