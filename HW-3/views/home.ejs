<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>ADS-SJSU</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
  :root{
    --bs-primary: #003E7E;
    --bs-primary-rgb: 0, 62, 126; 
  }
</style>

</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/">ADS-SJSU</a>
      <div class="ms-auto">
        <% if (user) { %>
          <a class="btn btn-outline-light me-2" href="/dashboard">Dashboard</a>
          <a class="btn btn-outline-light" href="/logout">Logout</a>
        <% } else { %>
          <a class="btn btn-light text-primary" href="/login">Login</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container py-5">
    <% if (alert) { %>
      <div class="alert alert-<%= alert.type %> mb-4"><%= alert.text %></div>
    <% } %>

    <div class="text-center">
      <h1 class="display-5 fw-bold text-primary">Welcome to the Department of Applied Data Science</h1>
      

      <% if (user) { %>
        <a class="btn btn-lg btn-warning text-dark mt-3" href="/dashboard">Go to your Dashboard</a>
      <% } else { %>
        <a class="btn btn-lg btn-primary mt-3" href="/login">Go to Login</a>
      <% } %>
    </div>
  </div>
</body>
</html>
